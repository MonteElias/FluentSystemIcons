<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FluentSystemIcons.Gallery.UserControls.IconCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentSystemIcons.Gallery.UserControls"
    xmlns:gallery="using:FluentSystemIcons.Gallery"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:DataType="local:IconCard"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid"
          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
          BorderThickness="1"
          CornerRadius="8"
          Padding="8"
          RowSpacing="8">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontFamily="{x:Bind Item.FontFamily, Mode=OneWay}"
                   Text="{x:Bind Item.Glyph, Mode=OneWay}"
                   FontSize="{x:Bind IconFontSize, Mode=OneWay}"
                   Margin="0,4"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

        <StackPanel Grid.Row="1" Spacing="2" HorizontalAlignment="Center">

            <!-- ======================================================= -->
            <!--     CAMBIO: Se usa DisplayName en lugar de Name         -->
            <!-- ======================================================= -->
            <TextBlock Text="{x:Bind Item.DisplayName, Mode=OneWay}"
                       FontWeight="Medium"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       TextTrimming="CharacterEllipsis"/>

            <TextBlock Text="{x:Bind Item.FontKey, Mode=OneWay}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       FontSize="12"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>